package com.kodilla.good.patterns.challenges;

public class TransactionRepository implements SaveTransaction {
    public boolean saveTransaction(Customer customer, Product product) {
        return true;
    }
}
